{% extends 'base.html.twig' %}

{% block body %}
<h1>Task List</h1>

<ul>
    {% for task in tasks %}
        <li>{{ task.taskname }}: {{task.description}} - {{ task.dateCompleted ? 'Done' : 'Pending' }}</li>
    {% endfor %}
</ul>
{% endblock %}
